[project]
name = "agent-harness"
version = "0.3.4"
description = "Reference implementation of an agent execution harness: explicit state machine, tool contracts, failure taxonomy, replayable traces."
authors = [{ name = "Shashi Shankar", email = "shashi@shashforge.dev" }]
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
